# News Extractor Recipe
name: browser_news
category: browser
role: news_extractor
model_layer: MIND

prompt_fragments:
  - "apps/prompts/browser/news.md"

input_docs:
  - path: "extraction_goal"
    path_type: "runtime"
    optional: false
    max_tokens: 200
  - path: "url"
    path_type: "runtime"
    optional: false
    max_tokens: 200
  - path: "content"
    path_type: "runtime"
    optional: false
    max_tokens: 3000

output_doc:
  path: "news_extraction.json"
  path_type: "temp"

token_budget:
  total: 5000
  prompt: 600
  input_docs: 3400
  output: 1000

description: |
  Extracts key information from news articles.

integration:
  invoked_by:
    - prose_extractor._extract_news
