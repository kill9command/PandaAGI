name: panda
channels:
  - conda-forge
dependencies:
  - python=3.11
  - fastapi>=0.111
  - uvicorn>=0.30
  - httpx>=0.27
  - pydantic>=2.6
  - ripgrep  # for code.search tool
  - pip
  - pip:
      - python-dotenv>=1.0
      - black>=24.4
      - ruff>=0.5
      - pytest>=8.2

